# Architecture Evidence

Capture `docs/architecture.md` and the Mermaid diagram in README. The architecture demonstrates CLI/API entry points reusing the same `ClassificationEngine` and modality classifier interface.
